apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: moodle
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # headless for stability
